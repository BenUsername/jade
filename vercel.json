{
    "version": 2,
    "builds": [
        { "src": "extension-server.js", "use": "@vercel/node" }
    ],
    "routes": [
        { "src": "/api/(.*)", "dest": "/extension-server.js" },
        { "src": "/(.*)", "dest": "/index.html" }
    ]
}